package org.allaymc.api.world.particle;

import java.awt.*;

/**
 * SplashParticle is a particle that shows up when a splash potion is splashed.
 *
 * @author daoge_cmd
 */
public record SplashParticle(Color color) implements Particle {
}
